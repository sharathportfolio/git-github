check the status 
-  git status

intilize repo
git init

branch 
- master or main

add all file
- git add . 

add one file
- git add <filename>

commit the changes
m -> message
- git commit -m "first commmit"

// doctor strange movie timeline
git branch


- git branch <newbranch> (create new branch)


- git checkout <newbranch> (switch branch)

git log --oneline

git branch
git branch bugfix
git switch bugfix
git log
git switch master
git switch -c dark-mode (create a branch & move there)
gti checkout -b pink-mode (create a branch & move there)

always commit before switcting to another branch

merge the branches


// dec 1
git diff
--- 
+++
changes file a & file b  

command
- git diff --staged
- git diff id1 id2 or git diff id1...id2
- git diff branch1 branch2 or git diff branch1...branch2

git stashing
- create a repowork & commit on main
- switch to another branch & work
- conflicting chnages do not allow to switch branch withouit commits
- stash is available to all branch
command
- git stash (you can switch branch)
- git stash pop (bring back those changes)
- git stash apply (appply changes & keep them in stash)
- git stash list 

more commends
- git checkout <hash> (detach head): new branch
- git switch main (re-attack head)
- git checkout HEAD~2 (look at 2 commit )
- git restore filename (get back to last commit version)
- git reflog
